function xxxcluster_fromdisk_uADZSZDA_pca_excerpt_0(fp,str_0,n_bin,d_1_);
if (~isempty(fp));
fprintf(fp,str_0);
for nb=0:n_bin-1; 
fprintf(fp,'%s',d_1_{1+nb}); 
if nb<n_bin-1; fprintf(fp,','); else fprintf(fp,';'); end; 
end; %for nb=0:n_bin-1; 
fprintf(fp,'\n');
end;%if (~isempty(fp));
if ( isempty(fp));
fprintf(str_0);
for nb=0:n_bin-1; 
fprintf('%s',d_1_{1+nb}); 
if nb<n_bin-1; fprintf(','); else fprintf(';'); end; 
end; %for nb=0:n_bin-1; 
fprintf('\n');
end;%if ( isempty(fp));
